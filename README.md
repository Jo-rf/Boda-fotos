# Boda-fotos
cd ruta/a/tu/carpeta/boda-fotos  # Ve a tu carpeta
git init  # Inicializa el repositorio
git add .  # Añade el archivo index.html
git commit -m "Subir página web de boda"
git branch -M main  # Crea la rama principal
git remote add origin https://github.com/TU-usuario/boda-fotos.git  # Usa tu enlace de GitHub
git push -u origin main  # Subir el repositorio
